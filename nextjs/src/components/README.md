# Components

Shared UI primitives for the Next.js migration live here.

Keep interactive components small and explicit about `"use client"`. Server-rendered page shells should stay under `src/app/`.
